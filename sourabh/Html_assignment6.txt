Q 6.1 When we link to an image from an HTML document, does the image have to be in the same
directory as the HTML page or can it exist anywhere on the Internet?
  ANS:- image pointing to <img> element in a web page and HTML link ...so we'll need to create ... In the same folder, create another file called images.html : <! ... in your own website, but hard-coding your domain name everywhere can make for some tricky situations.
